import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class CardModel {
  List<String> keyAccount;
  Color color;
  LinearGradient gradient;
  CardModel({
    required this.keyAccount,
    required this.color,
    required this.gradient,
  });
  
}
